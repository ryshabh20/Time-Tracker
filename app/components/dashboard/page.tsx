// import { chartsData } from "@/helper/charts";
import axios from "axios";

const dashboard = async () => {
  // const data = await chartsData();
  // console.log("this is your data", data);
  return <div>Dashboard</div>;
};

export default dashboard;
